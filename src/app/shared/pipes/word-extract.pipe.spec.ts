import { WordExtractPipe } from './word-extract.pipe';

describe('WordExtractPipe', () => {
  it('create an instance', () => {
    const pipe = new WordExtractPipe();
    expect(pipe).toBeTruthy();
  });
});
